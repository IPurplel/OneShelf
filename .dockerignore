# Only application source and its build recipe belong in the build context.
# Excludes local libraries, browser sessions, secrets, caches, and environments.
**
!app/
!app/**
!web/
!web/**
!requirements.txt
!deploy/
!deploy/Dockerfile
**/__pycache__/
**/*.py[cod]
